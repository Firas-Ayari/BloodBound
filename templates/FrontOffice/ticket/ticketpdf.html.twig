<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ event.title }}</title>
    <style>
        /* Add your CSS styles here */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f7e8e8;
        }
        .ticket-container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 30px;
            border: 1px solid #c65353;
            border-radius: 10px;
            box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.2);
        }
        .header-container {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 30px;
        }
        .logo-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background-color: #c65353;
        }
        .logo-container img {
            width: 80%;
            height: 80%;
            object-fit: contain;
        }
        .title-container {
            margin-left: 30px;
        }
        .ticket-title {
            font-size: 36px;
            margin-bottom: 10px;
            color: #c65353;
        }
        .details-container {
            display: grid;
            grid-template-columns: 1fr 2fr;
            row-gap: 20px;
            margin-bottom: 20px;
        }
        .details-label {
            font-size: 24px;
            font-weight: bold;
            color: #c65353;
        }
        .details-value {
            font-size: 24px;
            color: #ad1f1f;
        }
        .price-container {
            display: flex;
            align-items: center;
            justify-content: flex-end;
        }
        .ticket-price {
            font-size: 36px;
            color: #ad1f1f;
            margin-left: 10px;
        }
    </style>
</head>
<body>
    <div class="ticket-container">
        <div class="header-container">
            <div class="logo-container">
                
            </div>
            <div class="title-container">
                <h1 class="ticket-title">{{ event.title }}</h1>
            </div>
        </div>
        <div class="details-container">
            <div class="details-label">Event Date:</div>
            <div class="details-value">{{ event.eventDate ? event.eventDate|date('Y-m-d') : '' }}</div>
            <div class="details-label">Event Location:</div>
            <div class="details-value">{{ event.location }}</div>
            <div class="details-label">Start Time:</div>
            <div class="details-value">{{ event.startTime|date('H:i') }}</div>
            <div class="details-label">End Time:</div>
            <div class="details-value">{{ event.endTime|date('H:i') }}</div>
        </div>
        <div class="price-container">
            <div class="details-label">Ticket Price:</div>
            <div class="ticket-price">{{ ticket.price }}Dt</div>
    </div>
</body>
</html>



