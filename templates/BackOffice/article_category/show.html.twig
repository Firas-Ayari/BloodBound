{% extends 'base.html.twig' %}

{% block title %}ArticleCategory{% endblock %}

{% block body %}
    <h1>ArticleCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_category.id }}</td>
                
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ article_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_category_index') }}">back to list</a>

    <a href="{{ path('app_article_category_edit', {'id': article_category.id}) }}"><button class="btn btn-warning" style="background-color:darkorchid">edit</button></a>

    {{ include('backoffice/article_category/_delete_form.html.twig') }}
{% endblock %}
